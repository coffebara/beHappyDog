server:
  port: 8081

spring:
  #  로컬 db
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/behddb?serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: admin1234

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

# 파일 업로드 설정
#  servlet:
#    multipart:
#      enabled: true         # 파일 업로드 가능 여부
#      location: C:\\upload  # 업로드된 파일의 임시 저장 경로
#      max-request: 30MB     # 한번에 최대 업로드 가능 용량
#      max-file-size: 10MB   # 파일 하나의 최대 크기

  thymeleaf:
    cache: false

# Gmail 설정
  mail:
    host: smtp.gmail.com
    port: 587
    username: coffebara001@gmail.com
    password: wxpn likk xuwi pevv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging.level:
  org.hibernate.SQL: debug

# jwt Secret Key
secret-key: ThisStatementIsJwtSecretKeyDoNotUseThisStatement

